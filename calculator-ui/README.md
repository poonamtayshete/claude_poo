# Calculator UI

A React (Vite) single-page app providing a graphical calculator with the
same operations as `calculator.py` (add, subtract, multiply, divide).

## Development

```
cd calculator-ui
npm install
npm run dev
```

Then open the printed local URL in a browser.

## Build

```
npm run build
```

Outputs a production build to `dist/`.
